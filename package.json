{
  "name": "@noss-editor/monorepo",
  "private": true,
  "type": "module",
  "author": "Codefoxdev",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "test": "vitest",
    "check": "biome check",
    "fix": "biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "vitest": "^2.0.5"
  }
}
